<%= turbo_frame_tag "cart" do %>
  <!-- <h1>Cart</h1> -->
  <% if current_user.current_cart.orders.count > 0 %>
    <%= render "cart/orders" %>
  <% else %>
    <p>Your cart is empty</p>
  <% end %>
<% end %>